#version 450

out vec4 FragColour;

in vec3 colour;
in vec2 uvs;

uniform sampler2D baseTexture;
uniform sampler2D blendedTexture;

void main()
{
	FragColour = (texture(baseTexture, uvs) + texture(blendedTexture, uvs)) * 0.5;
}